import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ets-jogo-galo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
