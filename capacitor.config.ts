import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ets-tictactoe',
  appName: 'ets-tictactoe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
